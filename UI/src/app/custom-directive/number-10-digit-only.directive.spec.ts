import { Number10DigitOnlyDirective } from './number-10-digit-only.directive';

describe('Number10DigitOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new Number10DigitOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
